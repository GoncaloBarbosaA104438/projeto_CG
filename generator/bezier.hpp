#ifndef BEZIER_HPP
#define BEZIER_HPP

// Declaração da função que gera a superfície de Bezier a partir de um ficheiro .patch
void bezier(const char* patchFile, const char* outputFile, int tessellationLevel);

#endif